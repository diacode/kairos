defmodule Kairos.ScheduledReport.Scheduler do
  alias Kairos.ScheduledReport

  def schedule(scheduled_report) do
  end

  def unschedule(scheduled_report) do
  end

  def process_notification(scheduled_report) do
  end
end
